import KeyboardShortcuts

/// Global keyboard shortcut definitions for ContainerBar
extension KeyboardShortcuts.Name {
    /// Shortcut to toggle the ContainerBar menu
    static let toggleMenu = Self("toggleMenu")
}
